



/*
import {useState} from 'react'

const [searchTerm, setSearchTerm] = useState("");
onChange={event => {
    setSearchTerm(event.target.value)
    }}
  />
  {articles.filter((val)=>{
    /*Is the search term empty if yes run statement*//*
    if (searchTerm == "") {
      return val
    } 
    /*If the search is lowercase change everything to lowercase*//*
    else if (val.title.toLocaleLowerCase().includes(searchTerm.toLowerCase())) {
      return val
    }
  }).map((val, key)=>{
    return (
      <div className = "user" key={key}> 
       <p>{val.title}</p>
      </div>
    );
  })}
</div>
*/
